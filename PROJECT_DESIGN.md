# 民宿智能推荐与旅行规划平台——项目设计文档

> 文档状态：待确认  
> 当前版本：v0.2  
> 更新日期：2026-07-27  
> 项目代号：Stay Scale  
> 技术方向：Vue 3 + FastAPI  

## 1. 文档说明

本文档用于确认项目的产品定位、功能范围、技术架构、数据设计、AI能力、推荐逻辑和视觉规范。当前阶段只进行设计，不进入代码开发。

文档确认后，再依次进行数据库设计细化、接口设计、前后端项目骨架搭建和功能开发。后续如需求变化，应先更新本文档或对应的详细设计文档，再调整实现。

## 2. 项目概述

### 2.1 项目背景

用户在选择民宿时，通常需要在美团、途家、木鸟等多个平台之间反复搜索。不同平台存在名称不一致、房型描述不同、价格口径不同、优惠条件不同、评论分散等问题，使用户很难快速判断哪一家民宿真正适合自己。

与此同时，传统平台主要依靠目的地、日期、价格和评分等固定筛选项，难以理解“安静但不要太偏”“适合带老人”“可以贵一点，但卫生必须好”等模糊且个性化的需求。

本项目拟建设一个跨平台民宿决策与旅行规划平台，通过数据标准化、同店识别、房型匹配、价格比较、评论分析和AI交互，为用户提供可解释的个性化民宿推荐，并在选定住宿后生成与住宿位置相匹配的旅行方案。

### 2.2 产品定位

本项目定位为：

> 面向旅行者的跨平台民宿比价、智能推荐与旅行规划助手。

平台负责帮助用户理解和比较选择，不在第一阶段承担支付、退款、订单履约和售后服务。用户确认民宿后，跳转至原平台查看详情或完成预订。

### 2.3 核心价值

1. 将多个平台的分散民宿信息转换为统一、可比较的数据。
2. 识别不同平台中的同一家民宿和相同或相似房型。
3. 使用实际总价而非“每晚起价”进行比较。
4. 理解用户自然语言表达的旅行需求和隐含偏好。
5. 从大量评论中提取卫生、隔音、位置等真实体验。
6. 给出推荐理由、数据证据、潜在风险和必要妥协。
7. 将民宿选择与后续旅行行程连接起来。

### 2.4 产品原则

- 推荐少而精，优先展示3至5个有明显差异的候选方案。
- 价格、库存、距离和评分由数据与程序计算，不由AI猜测。
- AI负责理解、归纳和解释，不直接制造业务事实。
- 最低价不等于最推荐，综合考虑偏好、评价、位置和政策。
- 同房型、相似房型和不同房型必须明确区分。
- 推荐结论必须尽可能可解释、可追踪、可纠正。
- 第一阶段采用模块化单体，避免过早拆分微服务。

## 3. 项目目标与边界

### 3.1 第一阶段目标

第一阶段完成一个可演示的端到端闭环：

1. 用户输入目的地、日期、人数、预算和自然语言需求。
2. AI将自然语言转换为结构化偏好。
3. 系统从三平台样本数据中筛选候选民宿。
4. 系统合并同一家民宿并匹配相同或相似房型。
5. 系统统一计算入住期间实际总价。
6. 推荐引擎根据用户偏好生成个性化排序。
7. AI基于真实证据生成推荐理由和风险提示。
8. 用户通过对话修改偏好，系统重新筛选或排序。
9. 用户选中民宿后生成基础旅行行程。
10. 管理员可以审核同店匹配、房型匹配和AI分析结果。

### 3.2 第一阶段建议范围

- 选择1个示范城市。
- 模拟美团、途家、木鸟3个平台来源。
- 准备500至2000条平台民宿样本。
- 合并形成约200至500家统一民宿。
- 支持民宿、房型、价格快照、评论和设施数据。
- 支持规则加权推荐与AI需求解析。
- 支持地图展示和基础旅行规划。

### 3.3 第一阶段不包含

- 平台内支付和订单创建。
- 退款、改签和住宿售后。
- 未获授权的大规模实时平台采集。
- 覆盖全国所有城市和全部民宿。
- 复杂的会员积分、优惠券和营销系统。
- 基于大量真实用户行为训练的深度学习排序模型。
- 独立商家入驻、房态管理和结算系统。

## 4. 目标用户与典型场景

### 4.1 目标用户

#### 休闲旅行者

希望减少跨平台搜索时间，重视住宿体验和整体性价比。

#### 家庭与亲子用户

关注卫生、安全、空间、厨房、停车和儿童友好设施。

#### 携带老人出行的用户

关注电梯、楼层、交通、噪声、步行距离和就医便利度。

#### 情侣及审美型用户

关注设计风格、景观、私密性、拍照效果和周边氛围。

#### 预算敏感型用户

关注实际支付总价、平台优惠条件和取消政策。

### 4.2 典型用户需求

示例输入：

> 我和父母去大理住三晚，每晚最好不超过600元，不自驾，希望安静一点。老人上下楼不方便，附近吃饭要方便。

系统应识别：

- 硬条件：日期可订、入住人数满足、具有电梯或一楼房型。
- 预算条件：目标每晚600元以内，可配置允许小幅超出。
- 高权重偏好：安静、餐饮便利、公共交通便利。
- 风险厌恶：楼梯、偏远、夜间噪声。
- 低权重偏好：景观和网红属性。

## 5. 核心业务流程

### 5.1 智能推荐流程

1. 用户输入基础条件和自然语言需求。
2. AI解析目的地、日期、人数、预算、硬条件、软偏好和风险厌恶。
3. 用户确认或修改AI识别结果。
4. 后端按日期、人数和硬条件筛选民宿。
5. 推荐引擎计算特征、风险和综合得分。
6. 系统选择具有代表性的3至5个结果。
7. AI根据结构化证据生成推荐说明。
8. 前端展示匹配原因、风险、价格和平台来源。
9. 用户提出调整要求，系统更新条件并重新排序。

### 5.2 跨平台比较流程

1. 统一查询目的地、入住日期、退房日期和人数。
2. 各平台适配器将来源数据转换为统一格式。
3. 系统生成同店候选并计算同店匹配置信度。
4. 系统将确认匹配的平台房源归入统一民宿。
5. 系统在统一民宿内部进行房型匹配。
6. 计算各房型入住期间的实际总价与每晚均价。
7. 区分普通价、会员价、新客价和其他条件优惠。
8. 展示最低总价、取消条件、权益差异和数据更新时间。

### 5.3 旅行规划流程

1. 用户从推荐结果中选择民宿。
2. 系统根据民宿位置、旅行日期和用户偏好检索景点与餐饮候选。
3. 规则引擎先处理距离、开放时间、游玩时长和每日强度。
4. AI将结构化安排组织成易读的每日行程。
5. 用户可以要求“第二天轻松一些”或“增加一次日出”。
6. 系统局部调整相关日期，避免无必要地重建整个行程。

## 6. 功能架构

### 6.1 用户端功能

#### 首页与需求输入

- 目的地、日期、人数和预算输入。
- 自然语言旅行需求输入。
- 热门或示例需求快捷填充。
- 最近搜索和已保存方案入口。

#### AI推荐中心

- 展示AI提取的硬条件与软偏好。
- 允许用户直接修改识别结果。
- 展示3至5个推荐民宿。
- 展示推荐理由、风险提示和匹配证据。
- 支持多轮对话调整推荐。

#### 跨平台比价

- 展示同一家民宿的平台来源。
- 按同房型、相似房型、其他房型分组。
- 对比总价、每晚均价、取消政策和优惠条件。
- 标识总价最低、无需会员、政策最佳和综合推荐。
- 展示抓取或更新时刻。
- 跳转至来源平台。

#### 民宿详情

- 统一民宿基本信息。
- 平台页面与平台评分。
- 图片、设施、地址和地图。
- 房型和价格比较。
- 评论主题分析。
- 适合人群与潜在风险。
- 收藏、比较和生成行程。

#### 地图模式

- 民宿列表与地图联动。
- 地图标记显示价格或推荐状态。
- 支持区域、距离和交通条件筛选。
- 移动端使用全屏地图与底部抽屉。

#### 旅行规划

- 按日显示行程时间轴。
- 地图路线和预计交通时间。
- 景点、餐饮和休息安排。
- 预算估算。
- 对话式局部调整。
- 保存和分享方案。

#### 个人中心

- 用户资料和长期偏好。
- 历史搜索。
- 收藏民宿。
- 已保存的比较方案。
- 旅行计划。
- 推荐反馈记录。

### 6.2 管理端功能

- 用户与权限管理。
- 平台与数据源配置。
- 统一民宿管理。
- 平台民宿数据管理。
- 同店匹配审核。
- 房型匹配审核。
- 价格快照查看。
- 评论及分析结果查看。
- 推荐权重配置。
- AI提示词、模型版本和调用记录管理。
- 用户反馈与错误纠正。
- 数据更新任务和异常状态查看。

## 7. 技术架构

### 7.1 总体方案

采用前后端分离架构：

- 前端负责页面展示、交互、状态管理和地图联动。
- FastAPI负责业务接口、权限、推荐编排和AI调用。
- MySQL保存核心业务数据。
- MySQL空间类型、空间索引或经纬度距离公式支持范围和距离查询。
- Redis负责缓存、短期会话和任务状态。
- 异步任务处理数据更新、评论分析和匹配计算。
- 外部大模型或本地模型负责需求解析和文本分析。

第一阶段采用模块化单体。各业务模块共享一个后端应用和数据库，但保持明确的模块边界，便于后期按需要拆分。

### 7.2 前端技术选型

| 类别 | 建议方案 | 作用 |
| --- | --- | --- |
| 核心框架 | Vue 3 | 前端组件与页面 |
| 开发语言 | TypeScript | 类型安全和维护性 |
| 构建工具 | Vite | 开发与构建 |
| 路由 | Vue Router | 页面路由与权限控制 |
| 状态管理 | Pinia | 用户、搜索和推荐状态 |
| HTTP请求 | Axios | 调用后端接口 |
| UI组件 | Element Plus或Naive UI，待确认 | 基础表单与反馈组件 |
| 地图 | 高德地图JS API | 民宿与行程地图 |
| 图表 | ECharts，可选 | 评论维度和价格趋势 |

### 7.3 后端技术选型

| 类别 | 建议方案 | 作用 |
| --- | --- | --- |
| API框架 | FastAPI | REST API与业务编排 |
| 数据校验 | Pydantic | 请求、响应和AI输出校验 |
| ORM | SQLAlchemy 2 | 数据访问 |
| 数据迁移 | Alembic | 数据库结构版本管理 |
| 主数据库 | MySQL 8.0 | 核心业务数据 |
| 地理能力 | MySQL空间类型与空间索引 | 范围和距离查询 |
| 缓存 | Redis | 缓存、任务状态和会话 |
| 异步任务 | ARQ、RQ或Celery，待确认 | 评论分析和数据任务 |
| AI接入 | 独立AI服务层 | 统一模型调用与审计 |
| 部署 | Docker Compose | 本地与演示环境 |

### 7.4 后端逻辑模块

- 认证与权限模块。
- 用户与偏好模块。
- 搜索会话模块。
- 民宿数据中心。
- 平台数据适配器。
- 同店匹配模块。
- 房型匹配模块。
- 价格与库存模块。
- 评论分析模块。
- 推荐引擎模块。
- AI交互模块。
- 旅行规划模块。
- 收藏与历史模块。
- 管理后台模块。

## 8. 数据来源与接入设计

### 8.1 数据来源优先级

1. 平台开放接口、联盟接口或正式合作。
2. 民宿商家授权数据。
3. PMS或渠道管理系统接口。
4. 用户主动提交链接后的单次分析。
5. 获得授权的页面数据采集。
6. 第一阶段的公开、模拟或自建样本数据。

### 8.2 平台适配器

每个平台设置独立适配器，将来源数据转换为系统统一字段。推荐、比价和前端页面不应直接依赖某个平台的原始字段。

统一数据至少包含：

- 平台与平台房源ID。
- 名称、地址、经纬度。
- 民宿类型和经营属性。
- 房型、面积、床型、入住人数。
- 设施、图片和描述。
- 基础房价、清洁费、服务费和其他必选费用。
- 优惠条件、取消政策和库存状态。
- 平台评分、评论数和评论原文。
- 数据来源、查询条件和更新时间。

### 8.3 合规原则

- 未获授权时，不将大规模实时采集作为正式上线方案。
- 不采集与推荐无关的个人敏感信息。
- 保存数据来源、授权状态和更新时间。
- 展示平台内容时保留来源标识。
- 价格页面提示最终价格以原平台结算页为准。
- 商业上线前对平台条款、数据权益、著作权和个人信息保护要求进行专项评估。

## 9. 跨平台实体匹配设计

### 9.1 统一民宿模型

系统区分：

- 统一民宿：现实中被系统认定为同一个住宿实体。
- 平台民宿：统一民宿在某个平台上的具体页面。
- 平台房型：平台民宿下的具体售卖单元。

一个统一民宿可以关联多个平台民宿，一个平台民宿可以包含多个平台房型。

### 9.2 同店匹配流程

#### 候选召回

先通过城市、行政区、经纬度范围、地址关键词和名称关键词缩小候选集合，避免全量两两比较。

#### 多特征评分

建议初始权重：

```text
同店匹配分 =
25% 经纬度接近程度
+ 20% 地址相似度
+ 20% 名称相似度
+ 15% 图片相似度
+ 10% 设施与房型结构相似度
+ 10% 经营者等辅助信息
```

权重在获得人工标注样本后调整。

#### 置信度处理

- 大于等于0.90：可自动合并，并保留审计记录。
- 0.70至0.90：标记为疑似同店，进入人工审核。
- 小于0.70：默认视为不同民宿。

特别注意同一栋公寓、同一地址下的不同房东房源，不能仅凭经纬度自动合并。

### 9.3 房型匹配

房型匹配使用床型、面积、入住人数、景观、独立卫浴、图片、设施和权益等特征。

匹配结果分为：

- 同房型：允许直接比较价格。
- 相似房型：允许参考，必须显示差异。
- 不同房型：不进行最低价结论比较。

## 10. 价格比较设计

### 10.1 统一查询条件

所有比较必须基于：

- 相同入住日期和退房日期。
- 相同入住人数。
- 同房型或明确标记的相似房型。
- 可识别的相同或不同取消条件。
- 明确的用户身份与优惠资格。

### 10.2 总价计算

```text
实际总价 =
入住期间房费合计
+ 清洁费
+ 服务费
+ 必选附加费用
- 当前用户确定可用的优惠
```

同时展示：

- 实际总价。
- 每晚均价。
- 普通价格。
- 会员、新客和优惠券条件价格。
- 取消政策。
- 价格更新时间。

### 10.3 比较结论

系统可以分别提供：

- 无条件最低总价。
- 使用会员后的最低总价。
- 相同取消政策下的最低总价。
- 取消条件最宽松的平台。
- 综合性价比最高的平台或房型。

## 11. AI能力设计

### 11.1 AI职责

AI适合处理：

- 自然语言需求解析。
- 硬条件、软偏好和风险厌恶提取。
- 评论主题、情绪和风险归纳。
- 用户反馈理解。
- 推荐理由自然语言生成。
- 旅行方案表达和对话式调整。

AI不负责直接确定：

- 真实价格与库存。
- 经纬度和距离。
- 房型是否实际可订。
- 最终推荐分计算。
- 平台优惠是否对用户有效。

### 11.2 AI调用流程

1. 接收用户原始描述。
2. AI输出结构化需求。
3. Pydantic校验字段与类型。
4. 用户确认关键条件。
5. 推荐引擎查询并排序候选。
6. 后端生成结构化推荐证据。
7. AI将证据组织成推荐说明。
8. 后端校验引用的数字和事实。
9. 前端展示结果。

### 11.3 评论分析

评论建议拆分为以下主题：

- 卫生。
- 隔音。
- 位置与交通。
- 服务。
- 设施。
- 图片真实性。
- 老人友好。
- 儿童友好。
- 周边餐饮。
- 入住与退房体验。

分析时考虑评论时间、评论数量、内容完整度、重复内容和异常发布模式。样本量不足时应显示“证据不足”，而不是给出确定结论。

### 11.4 AI安全与可信约束

- AI输出使用固定结构，由后端校验。
- 推荐文案中的数值只能引用后端提供的证据。
- 保存模型、提示词版本、输入摘要和输出结果。
- AI失败时退化为规则推荐和模板解释。
- 评论摘要保留对应评论证据或统计依据。
- 不向模型发送无关的用户隐私信息。
- 涉及价格与库存时必须显示数据更新时间。

## 12. 推荐引擎设计

### 12.1 推荐步骤

1. 日期、库存、人数和硬条件过滤。
2. 计算位置、价格、设施、评价和风险特征。
3. 根据用户偏好动态调整权重。
4. 计算综合推荐分。
5. 加入结果多样性，避免前几名高度相似。
6. 输出排名、正面证据、负面证据和缺失信息。

### 12.2 初始评分模型

```text
综合推荐分 =
30% 用户偏好匹配
+ 20% 价格优势
+ 15% 位置与交通
+ 15% 评论质量
+ 10% 设施匹配
+ 10% 取消政策
- 风险扣分
```

不同人群动态调整权重。例如带老人出行时，提高楼层、电梯、噪声和交通权重；亲子旅行时，提高卫生、安全、空间和厨房权重。

### 12.3 推荐解释结构

每个推荐结果需要回答：

1. 为什么适合当前用户。
2. 相比其他候选有什么优势。
3. 存在哪些风险或妥协。
4. 推荐依据是否充分。
5. 哪个平台和房型更值得选择。

### 12.4 后期演进

第一阶段使用规则与加权排序。积累点击、收藏、比较、跳转、隐藏和反馈数据后，可以加入学习排序模型，但仍保留规则过滤与解释证据。

## 13. 数据模型概览

### 13.1 核心实体

| 实体 | 说明 |
| --- | --- |
| User | 用户账号与基础资料 |
| UserPreference | 用户长期偏好 |
| SearchSession | 一次搜索或AI推荐会话 |
| Platform | 平台信息 |
| CanonicalListing | 统一民宿 |
| PlatformListing | 某平台上的民宿页面 |
| RoomType | 某平台下的房型 |
| PriceSnapshot | 指定查询条件下的价格快照 |
| Facility | 标准化设施 |
| Review | 平台评论 |
| ReviewInsight | 评论主题与分析结果 |
| ListingMatch | 同店匹配记录 |
| RoomMatch | 房型匹配记录 |
| RecommendationResult | 推荐分、排名和证据 |
| Favorite | 用户收藏 |
| TripPlan | 旅行方案 |
| ItineraryDay | 每日行程 |
| AIInvocation | AI调用审计记录 |

### 13.2 关键关系

- 一个用户拥有多个偏好、搜索会话、收藏和旅行方案。
- 一个统一民宿关联多个平台民宿。
- 一个平台民宿包含多个房型和评论。
- 一个房型拥有多个价格快照。
- 一个搜索会话产生多个推荐结果。
- 一个旅行方案包含多个每日行程。
- 同店匹配和房型匹配均保存置信度、依据和审核状态。

### 13.3 MySQL主要保存的数据

MySQL作为项目的业务事实库，主要保存以下七类数据：

#### 用户与权限数据

- 用户账号、密码哈希、头像和联系方式。
- 用户角色、账号状态和登录时间。
- 用户长期偏好，例如预算区间、出行人群和设施偏好。
- 用户收藏、历史搜索和对推荐结果的反馈。

不保存明文密码。第三方登录令牌和其他敏感凭据应加密保存或交由专门的密钥配置管理。

#### 统一民宿数据

- 系统内部统一民宿ID。
- 标准名称、民宿类型、简介和经营状态。
- 标准地址、行政区域、经纬度和地理坐标。
- 设施、适合人群、入住规则和统一图片引用。
- 数据完整度、可信度和最后更新时间。

统一民宿代表现实中的一家民宿，是跨平台比较的主体。

#### 平台民宿与房型数据

- 平台名称、平台房源ID和原页面链接。
- 平台展示名称、地址、评分和评论数量。
- 平台房型名称、床型、面积、入住人数和景观。
- 早餐、取消条件、增值权益和房间设施。
- 原始数据摘要、来源状态和更新时间。

同一个统一民宿可以关联美团、途家、木鸟等多个平台房源。各平台房型继续独立保存，避免错误地覆盖差异。

#### 价格与库存快照

- 查询的入住日期、退房日期和入住人数。
- 每晚房价、清洁费、服务费和必选费用。
- 普通价、会员价、新客价及其适用条件。
- 计算后的入住总价和每晚均价。
- 可订状态、剩余房量或平台返回的库存状态。
- 数据采集或接口查询时间。

价格不能只保留一个当前值。应按查询条件保存快照，才能知道该价格对应的日期、人数、房型和优惠资格。过期快照可按保留策略归档或清理。

#### 评论与评论分析数据

- 评论来源平台、平台评论ID和评论时间。
- 评分、评论文本、图片引用和有用数等公开字段。
- 卫生、隔音、位置、服务等主题分析结果。
- 正面或负面倾向、风险标签和分析置信度。
- 使用的AI模型、分析版本和分析时间。

评论原文与AI分析结果分开保存。当分析规则或模型更新时，可以重新计算分析结果而不覆盖原始评论。

#### 匹配与推荐数据

- 同店候选、匹配分、特征证据和审核状态。
- 房型匹配分、差异字段和人工确认结果。
- 一次推荐使用的用户条件、权重和算法版本。
- 推荐候选、最终排名、综合得分和分项得分。
- 正面依据、风险依据和用户反馈。

保存推荐快照可以解释“系统当时为什么推荐这家”，也便于后期评估和优化算法。

#### 旅行规划数据

- 旅行目的地、日期、人数、预算和总体偏好。
- 用户选择的民宿。
- 每日行程、景点、餐饮和交通安排。
- 预计到达时间、停留时间和预算。
- 行程版本、用户调整记录和保存状态。

### 13.4 不建议直接存入MySQL的数据

- 大量民宿原图和评论图片：保存到对象存储或文件服务，MySQL只保存URL、哈希和元数据。
- Redis缓存内容：短期推荐进度、验证码、热点查询和临时会话由Redis保存。
- 应用密钥和平台密钥：通过环境配置或密钥服务管理，不写入普通业务表。
- 大体积采集原始文件：放入独立文件存储，MySQL只保存来源、文件位置和处理状态。
- 运行日志：写入日志系统或日志文件，关键审计记录再保存到数据库。

### 13.5 MySQL使用建议

- 使用MySQL 8.0及以上版本和`utf8mb4`字符集。
- 金额使用`DECIMAL`，不使用浮点数，避免价格计算误差。
- 日期、时间和数据更新时间使用明确的时区规则。
- 经纬度可分别保存，也可增加`POINT`字段和空间索引。
- 设施、平台、民宿和房型之间的关系使用关联表，避免把重要查询字段全部塞进JSON。
- AI证据、扩展属性等结构不固定的内容可以使用JSON字段，但核心筛选字段仍应使用普通列。
- 为平台房源ID、统一民宿ID、城市、日期、房型和任务状态建立必要索引。
- 对价格快照、评论和AI调用记录制定归档或清理策略，避免数据无限增长。

## 14. API领域划分

接口统一使用 `/api/v1` 版本前缀，建议按领域划分：

| 接口领域 | 主要职责 |
| --- | --- |
| `/auth` | 注册、登录和令牌刷新 |
| `/users` | 用户资料和长期偏好 |
| `/search` | 条件搜索与搜索会话 |
| `/ai/requirements` | AI需求解析与确认 |
| `/recommendations` | 推荐创建、查询和调整 |
| `/listings` | 统一民宿列表与详情 |
| `/comparisons` | 跨平台房型和价格比较 |
| `/reviews` | 评论与分析结果 |
| `/trips` | 旅行规划与调整 |
| `/favorites` | 收藏管理 |
| `/admin` | 后台审核和配置 |

长耗时任务建议采用“创建任务、返回任务ID、查询进度”的模式。第一阶段可使用轮询，后期可升级为SSE流式进度通知。

## 15. UI与品牌设计规范

### 15.1 视觉定位

整体方向：

> 自然旅居 + 编辑式旅行杂志 + 清晰可信的数据比较。

页面需要同时传达民宿旅行的温度与比价推荐的理性，避免传统OTA式高饱和蓝色，也避免通用AI产品常见的紫色渐变和机器人形象。

### 15.2 品牌色彩

| 用途 | 名称 | 色值 |
| --- | --- | --- |
| 品牌主色 | 山岚青 | `#245A50` |
| 主色悬停 | 深松绿 | `#19463F` |
| 行动强调 | 陶土橙 | `#D86B3D` |
| 页面背景 | 暖米白 | `#F5F0E6` |
| 卡片背景 | 象牙白 | `#FFFCF6` |
| 主文字 | 墨黑绿 | `#202A27` |
| 次级文字 | 灰褐色 | `#6F756F` |
| 边框 | 浅砂岩 | `#DDD6C9` |
| 成功或优势 | 苔藓绿 | `#488565` |
| 警告或不确定 | 琥珀黄 | `#D99A32` |
| 错误或负面 | 砖红色 | `#B84D3E` |

建议色彩比例：背景与卡片约70%，山岚青约20%，陶土橙与状态色约10%。

### 15.3 字体方向

- 标题：思源宋体或具备旅行编辑感的授权字体。
- 正文与交互：思源黑体。
- 数字与价格：可搭配数字清晰的西文字体。
- 宋体仅用于大标题和章节标题，长正文使用黑体。

正式上线前确认所有Web字体授权。

### 15.4 页面设计方向

#### 首页

- 非对称的旅行杂志式布局。
- 大标题与AI需求输入作为视觉核心。
- 使用目的地图片拼贴而非传统轮播图。
- 背景可加入极淡的等高线、地图线路或纸张纹理。

#### 推荐列表

- 图片、匹配理由和风险提示同等重要。
- 推荐分配合证据展示，不使用夸张仪表盘。
- 卡片突出实际总价和平台差异。

#### 比价页面

- 信息密度高于其他页面，优先保证清晰和可信。
- 同房型与相似房型分组展示。
- 最低价、政策最佳和综合推荐使用不同标识。
- 所有条件价明确说明资格。

#### 地图模式

- 桌面端采用左侧列表、右侧固定地图。
- 移动端采用列表与全屏地图切换。
- 卡片悬停、选择与地图标记保持联动。

#### AI形象

- 使用路线、指南针、地图定位和旅行便签语言。
- 不使用机器人头像或紫色发光球。
- 展示AI处理阶段和已完成的数据步骤，增强可信度。

### 15.5 组件规范

- 卡片圆角建议12至16px。
- 按钮圆角建议8至10px。
- 标签可使用胶囊形，但只承载短状态。
- 主要通过边框和背景区分层级，阴影保持克制。
- 图片圆角与卡片保持一致。
- 首页留白较大，比价表格更紧凑。
- 使用统一线性图标。
- 避免大面积玻璃拟态和过度半透明效果。

### 15.6 响应式要求

- 支持桌面、平板和手机。
- 移动端优先保留搜索、推荐理由、总价和主要操作。
- 宽表格在移动端转换为分组卡片或横向比较抽屉。
- 地图在移动端使用全屏模式。
- 关键按钮满足触控尺寸要求。

## 16. 非功能需求

### 16.1 性能

- 普通列表接口目标响应时间小于1秒，不含外部平台实时请求。
- 推荐任务应先返回处理状态，避免长时间阻塞页面。
- 缓存常用城市、民宿基础数据和短期价格结果。
- 评论分析、图片匹配和批量数据更新异步执行。

### 16.2 可靠性

- 外部平台或AI服务不可用时支持降级。
- 推荐核心流程可退化为规则排序。
- AI解释失败时使用结构化模板展示证据。
- 所有异步任务保留失败原因并支持重试。

### 16.3 安全

- 密码采用安全哈希，不保存明文。
- 使用访问令牌和角色权限控制。
- 管理接口与普通用户接口隔离。
- 对用户输入、上传内容和AI输出进行校验。
- 对AI调用、匹配审核和关键配置变更保留日志。

### 16.4 可观测性

- 记录接口错误、任务状态和外部服务异常。
- 记录推荐版本、权重和使用的数据快照。
- 记录AI模型、提示词版本、耗时和失败类型。
- 统计搜索、推荐点击、收藏、比较和跳转行为。

## 17. 项目实施阶段

### 阶段一：基础数据与比价闭环

- 确定示范城市与样本数据。
- 建立统一民宿、平台民宿、房型和价格模型。
- 完成基础查询、同店关联和价格比较。
- 完成首页、列表、详情和比价页面。

### 阶段二：AI智能推荐

- 自然语言需求解析。
- 硬条件和软偏好确认。
- 规则加权推荐。
- 评论主题分析。
- 推荐理由和风险提示。
- 对话式偏好调整。

### 阶段三：自动匹配与管理审核

- 同店候选召回与评分。
- 房型自动匹配。
- 匹配置信度与证据展示。
- 管理后台人工审核与纠错。

### 阶段四：旅行规划

- 地图和周边内容数据。
- 按日行程生成。
- 路线、时间和预算估算。
- 行程局部调整和保存分享。

### 阶段五：上线增强

- 接入正式授权数据源。
- 平台联盟跳转。
- 推荐效果评估与学习排序。
- 监控、限流、成本和合规优化。

## 18. 第一阶段验收标准

满足以下条件可视为第一阶段核心闭环完成：

1. 用户可以输入基础条件和一段自然语言需求。
2. 系统能展示并允许修改AI解析出的偏好。
3. 系统能从至少三个模拟平台查询候选民宿。
4. 同一家民宿能合并显示多个平台来源。
5. 相同与相似房型能被明确区分。
6. 系统能按统一口径计算并比较总价。
7. 推荐结果包含分数、正面证据和风险提示。
8. 用户修改偏好后，推荐结果能够重新排序。
9. 民宿详情能够展示地图和评论分析。
10. 用户可以选中民宿并生成基础旅行计划。
11. 管理员可以审核同店和房型匹配结果。
12. 页面在桌面端和移动端均可正常使用。

## 19. 项目效果指标

第一阶段主要评估系统正确性和可用性：

- AI需求字段解析准确率。
- 同店自动匹配准确率。
- 房型匹配准确率。
- 总价计算正确率。
- 推荐解释事实一致率。
- 推荐结果点击率。
- 用户调整偏好的次数与成功率。
- 收藏、比较和跳转率。
- 推荐任务平均完成时间。

若为毕业设计，可重点展示匹配准确率、推荐算法设计、评论分析效果和完整业务闭环。

## 20. 待确认事项

以下事项需要在开始写代码前确认：

1. 项目性质：毕业设计、作品集还是计划商业上线。
2. 第一阶段示范城市。
3. 数据来源：纯模拟、自建样本、公开数据或已有授权数据。
4. 第一阶段是否必须接入真实大模型API。
5. 前端基础组件库选择Element Plus还是Naive UI。
6. 是否需要普通用户注册登录，还是先允许游客使用。
7. 第一阶段旅行规划做到基础按日方案，还是包含实时天气和路线接口。
8. 是否将管理后台纳入第一阶段演示范围。
9. 第一阶段部署目标：仅本地运行、服务器部署或公开演示站点。
10. 项目最终需要配套哪些材料，例如论文、答辩PPT、接口文档和测试报告。

## 21. 建议的下一步

本文档确认后，按以下顺序进入详细设计与实现：

1. 确认第20节的关键决策。
2. 输出页面信息架构与关键页面低保真原型。
3. 输出数据库表结构详细设计。
4. 输出API请求与响应契约。
5. 确定前后端目录结构和编码规范。
6. 搭建Vue 3与FastAPI项目骨架。
7. 使用样本数据完成第一条端到端业务链路。

## 22. 模块化开发与逐次验收机制

### 22.1 开发原则

项目采用“单模块开发、单模块交付、用户验收后继续”的方式推进，不一次性完成全部项目。

具体规则如下：

1. 每次只实现一个已经确认范围的模块。
2. 开始模块前，先说明目标、涉及页面或接口、预计改动和验收方法。
3. 模块开发完成后，先执行自动测试和必要的人工检查。
4. 每次交付均列出已完成内容、改动文件、运行方式、测试结果和已知限制。
5. 交付后停止继续开发，等待用户明确验收。
6. 用户回复“验收通过”后，才进入下一个模块。
7. 用户提出问题时，只修改当前模块并重新提交验收。
8. 未经确认，不提前开发后续业务模块。
9. 已验收模块的公共接口和数据结构原则上保持稳定；确需变更时，提前说明影响。
10. 需求变化先更新设计或模块说明，再实施代码修改。

### 22.2 模块状态

每个模块使用以下状态：

| 状态 | 含义 |
| --- | --- |
| 待设计 | 尚未确定详细范围 |
| 待确认 | 已给出模块方案，等待用户确认 |
| 开发中 | 用户已同意，正在实现 |
| 待验收 | 已完成测试，等待用户检查 |
| 修改中 | 根据验收意见修正 |
| 已通过 | 用户已明确验收通过 |

任何时刻原则上只允许一个模块处于“开发中”或“修改中”。

### 22.3 建议的模块拆分与顺序

#### M0：工程骨架与运行环境

范围：

- Vue 3、TypeScript和Vite前端骨架。
- FastAPI后端骨架。
- MySQL和Redis本地运行配置。
- 环境变量示例。
- 前后端健康检查。
- 基础代码格式与目录规范。

验收重点：前端、后端、MySQL和Redis可以按说明启动；前端能够访问后端健康检查；不包含业务页面。

#### M1：MySQL数据模型与演示数据

范围：

- 第一批核心表结构。
- SQLAlchemy模型与Alembic迁移。
- 民宿、平台、房型和价格演示数据。
- 数据初始化方式。

验收重点：数据库可从空库完成迁移和初始化；能正确表达统一民宿、平台民宿、房型和价格快照关系。

#### M2：前端视觉基础与页面框架

范围：

- 品牌色、字体、间距和响应式变量。
- 通用按钮、标签、卡片和状态组件。
- 首页、推荐、比价、详情、行程和个人中心的空页面路由。
- 桌面端与移动端基础导航。

验收重点：视觉方向符合山岚青、陶土橙和暖米白方案；页面框架响应式正常；不填充虚假的完整业务功能。

#### M3：民宿查询与展示闭环

范围：

- 后端民宿列表和详情接口。
- 目的地、价格、人数等基础筛选。
- 前端搜索、列表和详情展示。
- 加载、空数据和失败状态。

验收重点：前端数据全部来自FastAPI和MySQL；修改数据库内容后页面能够反映变化。

#### M4：跨平台同店匹配

范围：

- 同店候选召回。
- 名称、地址和距离等特征评分。
- 匹配置信度与证据。
- 基础人工确认接口。

验收重点：使用标注样本验证匹配结果；同地址不同房东不会仅凭位置自动合并；能够查看匹配依据。

#### M5：房型匹配与价格比较

范围：

- 同房型、相似房型和不同房型判断。
- 总价和每晚均价计算。
- 普通价与条件优惠区分。
- 前端跨平台比价页面。

验收重点：不同房型不直接得出最低价结论；费用计算准确；页面明确展示权益和取消条件差异。

#### M6：AI需求解析

范围：

- 自然语言需求输入。
- 结构化提取日期、预算、硬条件、软偏好和风险厌恶。
- Pydantic输出校验。
- 用户确认和修改识别结果。
- AI不可用时的降级处理。

验收重点：AI解析结果可见、可改、可保存；错误输出不会直接进入业务计算；关闭AI后基础筛选仍可使用。

#### M7：个性化推荐引擎

范围：

- 硬条件过滤。
- 推荐特征和分项评分。
- 动态权重和风险扣分。
- 结果多样性。
- 结构化推荐证据。

验收重点：相同输入得到相同结果；修改偏好后排序合理变化；综合分可以拆解并追踪。

#### M8：AI推荐解释与对话调整

范围：

- 基于结构化证据生成推荐说明。
- 正面理由、风险和妥协说明。
- 用户反馈解析与重新排序。
- 数值事实校验和模板降级。

验收重点：AI不能生成证据中不存在的价格、距离和评论结论；用户改变偏好后后端真正重新计算。

#### M9：评论分析

范围：

- 评论数据读取与基础清洗。
- 卫生、隔音、位置等主题分析。
- 评论证据、置信度和分析版本。
- 民宿详情页评论洞察。

验收重点：评论原文与分析结果分离；样本不足时显示证据不足；分析结果能够追溯到评论证据。

#### M10：地图与位置能力

范围：

- 民宿经纬度与距离查询。
- 地图标记和民宿列表联动。
- 桌面端分栏地图。
- 移动端全屏地图与底部抽屉。

验收重点：点击或悬停列表时地图同步；距离计算合理；桌面端和移动端均可操作。

#### M11：旅行规划

范围：

- 用户选择民宿后创建旅行方案。
- 按日行程、景点、餐饮和交通安排。
- 时间、距离和基础预算约束。
- 用户局部调整与版本保存。

验收重点：行程围绕实际民宿位置生成；调整单日安排时不无故覆盖其他日期；方案可保存和再次打开。

#### M12：用户系统与个人中心

范围：

- 注册、登录和JWT认证。
- 用户资料与长期偏好。
- 收藏、历史搜索和已保存方案。
- 普通用户和管理员角色。

验收重点：权限边界正确；用户只能访问自己的私人数据；登录失效和异常状态处理完整。

#### M13：管理审核后台

范围：

- 民宿与平台数据查看。
- 同店和房型匹配审核。
- 评论分析查看。
- 推荐权重和任务状态管理。
- 关键操作审计。

验收重点：管理员能够确认、驳回和纠正匹配；普通用户不能访问管理接口；操作结果能影响后续业务数据。

#### M14：测试、部署与最终交付

范围：

- 核心单元测试和集成测试补全。
- 端到端主流程测试。
- Docker Compose运行方案。
- 初始化数据、部署说明和故障排查说明。
- 最终演示检查。

验收重点：新环境可按文档启动；自动测试通过；可完整演示搜索、比价、AI推荐、调整、旅行规划和后台审核。

### 22.4 每个模块的交付格式

每次模块完成后，交付说明统一包含：

1. 本模块实现目标。
2. 已完成的功能。
3. 明确未包含的内容。
4. 新增或修改的文件。
5. 数据库或接口变化。
6. 自动测试和检查结果。
7. 用户可执行的验收步骤。
8. 已知限制或待讨论事项。
9. 当前模块状态：待验收。

### 22.5 验收处理规则

- 用户回复“验收通过”：当前模块标记为已通过，可以讨论下一模块。
- 用户回复修改意见：当前模块进入修改中，只处理本模块问题。
- 验收发现前序模块缺陷：记录关联模块和影响范围，先说明修复方案，再修改。
- 新需求超出当前模块：加入需求清单，不在当前模块中临时扩张范围。
- 影响数据库或公共接口的修改：先说明兼容性和迁移方案，得到确认后实施。

### 22.6 首个开发模块

设计文档整体确认后，首先进入M0“工程骨架与运行环境”。开始前仅提交M0的详细实施清单和验收步骤，用户确认后再创建Vue与FastAPI代码。

---

本文档在用户确认前不作为最终开发基线，当前不进入代码实现阶段。
