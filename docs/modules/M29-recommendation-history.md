# M29 推荐历史与旅行方案管理（第一期）

M29 将登录用户与新生成的推荐会话关联，并提供历史查询：

```text
GET /api/v1/recommendations/history
```

未登录用户仍可匿名获取推荐，但匿名会话不会出现在个人历史中。登录用户在个人中心可以查看城市、日期和结果数量，并进入对应推荐会话。历史最多返回最近 50 条，后续再接入旅行计划保存和删除能力。
